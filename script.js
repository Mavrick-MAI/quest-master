
    // Create a Vue app
    const app = Vue.createApp({
        data() {
          return {
            message: 'Hello, World!'
          };
        }
      });
  
      // Mount the app
      app.mount('#app');